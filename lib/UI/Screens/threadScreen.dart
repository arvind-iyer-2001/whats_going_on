import 'package:flutter/material.dart';

class ThreadScreen extends StatelessWidget {
  static const routeName = 'Thread Screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
